# Dependencies (reinstall with: npm install)
node_modules/

# Next.js build output (regenerate with: npm run build)
.next/
out/

# TypeScript build cache
tsconfig.tsbuildinfo

# OS
.DS_Store
Thumbs.db

# Local env (never commit — contains local-only secrets)
.env.local